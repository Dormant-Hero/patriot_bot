# Step 1:

<del>Use the database library to load commands and embed commands already available in the database</del>
edit: Opted to put database logic in its own db.py file at the top level. This is because some of the code could
be repeated and to some extent seems to be better common practice.

Also moved constants to a config file.

# Step 2:

<del>Create functionality that adds/deletes commands. Both will need to restart 
the bot to re-apply the commands quickly. Also I should research if I have to restart the cog or bot to reload new commands. </del>

# Overall Guidelines:

<del>-- Consider moving DB logic to it's own file.</del>
<del>-- No AI just google without it's AI to really encourage personal growth and show what I am capable of. </del>
-- Consider if I need command_name.lower() for everywhere I have that.

# Over minor todos
Make it so the embed colour argument/param has actual word choices for the colours one can select as well as a default if not already.

Make a full on help command with search and paged embed.


# Notes So Far:

-- Installed pyscopg binary as the documentation states it has the best performance and does not need local libpg and build tools.